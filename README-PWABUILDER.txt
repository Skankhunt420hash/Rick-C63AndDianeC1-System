PWABuilder-Upload-Paket fuer Rick-C63AndDianeC1.

Nutze diese Dateien fuer PWABuilder bzw. hoste sie statisch und gib PWABuilder die URL.
Hinweis: Das ist ein PWA-Paket fuer APK-Erzeugung ueber PWABuilder, kein lokal gebautes APK.
